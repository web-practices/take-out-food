class Promotion {
  constructor(order) {
    this.halfPriceDishes = ["ITEM0001", "ITEM0022"];
    this.order = order;
  }

  discount() {
    // Need to be implement
  }
}

module.exports = Promotion;
